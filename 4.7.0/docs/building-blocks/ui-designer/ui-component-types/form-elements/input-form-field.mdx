---
title: Input Form Field
description: Input form fields allow users to enter and validate data in your FlowX applications. Learn how to configure and customize input fields with validation, styling, and platform-specific settings.
---

## Overview

Input form fields are essential UI components that enable users to enter data in your applications. They support various data types, validation rules, and can be customized for different platforms.

<Frame>
![Input Field Example](https://s3.eu-west-1.amazonaws.com/docx.flowx.ai/release40/input_element.png)
</Frame>

## Basic Configuration

### Process Data Key

The process data key creates a binding between the input field and your process data. This enables you to:

- Reference the input value in [decisions](../../../node/exclusive-gateway-node)
- Use the data in [business rules](../../../actions/business-rule-action/business-rule-action)
- Pass values to [integrations](../../../node/message-send-received-task-node#from-integration)

<Info>
Choose descriptive keys that reflect the data's purpose. For example, use `customer.firstName` instead of generic keys like `input1`.
</Info>

### Core Properties

Configure these essential input field properties:

- **Label**: The text displayed above the input field
- **Placeholder**: Helper text shown when the field is empty
- **Type**: The input data type (text, number, email, password)
- **Prefix**: Text or symbol displayed before the input
- **Suffix**: Text or symbol displayed after the input
- **Has Clear**: Enable a button to clear the field's content
- **Helper Text**: Additional guidance, optionally shown in an info tooltip
- **Update on Blur**: Whether to update the value when focus leaves the field

<Tip>
Use clear, concise labels and helpful placeholder text to guide users on what information to enter.
</Tip>

## Data Configuration

### Default Values

Set initial values for input fields using the datasource configuration:

<Frame>
![Datasource Configuration](https://s3.eu-west-1.amazonaws.com/docx.flowx.ai/release40/input_datasource1_new.png)
</Frame>

<Frame>
![Default Value Settings](https://s3.eu-west-1.amazonaws.com/docx.flowx.ai/release40/input_datasource2.png)
</Frame>

### Computed Values

For dynamic default values:

1. Enable the "Computed value" option (f(x) icon)
2. Enter a JavaScript expression to calculate the value
3. The value updates automatically based on your expression

<Card title="Learn More About Computed Values" href="../../dynamic-and-computed-values#computed-values" icon="calculator">
Read about creating dynamic computed values
</Card>

## Validation

### Adding Validators

Input validation ensures data quality and provides user feedback. Configure validators to:

- Require field completion
- Check data format (email, numbers, etc.)
- Enforce length or value constraints
- Implement custom validation rules

<Frame>
![Validator Example](https://s3.eu-west-1.amazonaws.com/docx.flowx.ai/release40/Input_validat.gif)
</Frame>

<Card title="Validation Documentation" href="../../validators" icon="shield-check">
Learn more about available validators and custom validation
</Card>

## Dynamic Behavior

### Hide/Disable Expressions

Control input field visibility and state using JavaScript expressions:

#### Hide Conditions
```javascript
// Example: Hide field if income is empty
${application.input.income} === null || ${application.input.income} === ""
```

<Frame>
![Hide Expression Result](https://s3.eu-west-1.amazonaws.com/docx.flowx.ai/release40/input_hide_result.gif)
</Frame>

#### Disable Conditions
```javascript
// Example: Disable when name equals 'TEST'
${application.input.name} == 'TEST'
```

<Warning>
Always configure the same hide expression under both expressions → hide and the disabled condition to maintain consistency.
</Warning>

## UI Actions

Configure input behavior with UI actions:

1. Select the trigger event (currently supports `CHANGE`)
2. Choose the action type
3. Configure action parameters

<Frame>
![UI Actions Configuration](https://s3.eu-west-1.amazonaws.com/docx.flowx.ai/release40/input_ui_actions_new.gif)
</Frame>

<Card title="UI Actions Guide" href="../../ui-actions" icon="bolt">
Detailed information about configuring UI actions
</Card>

## Platform-Specific Configuration

### Settings Overrides

Customize input behavior per platform (Web, iOS, Android):

- **Properties**: Label, helper text, placeholder, prefix, suffix
- **Expressions**: Hide conditions
- **Styling**: Platform-specific appearance

<Tip>
Use the import/push feature to copy overrides between platforms:
![Override Import/Push](https://s3.eu-west-1.amazonaws.com/docx.flowx.ai/release40/importing_pushing_overrides.gif)
</Tip>

## Styling Configuration

### Web Styling

Configure web-specific appearance:

#### Icons
- Left and right icons with customizable properties
- Icon selection from Media Library

#### Sizing
- **Fill**: Expands to container width
- **Fixed**: Maintains specific width
- **Auto**: Adjusts to content

### Mobile Styling

Additional options for iOS and Android:

- **Height**: Platform-specific units (pt for iOS, dp for Android)
- Platform-specific style overrides

## Style Overrides

Customize component appearance with theme overrides:

<AccordionGroup>
<Accordion title="Common Properties">
- Border radius and width
- Text styling
</Accordion>

<Accordion title="State-Specific Styling">
Configure colors and styles for:
- Empty state
- Active state
- Filled state
- Disabled state
- Error state
- Hover state (Web only)
</Accordion>

<Accordion title="Text Elements">
Style options for:
- Labels
- Helper text
- Error messages
- Placeholders
</Accordion>
</AccordionGroup>

<Info>
Theme overrides can be imported between platforms to maintain consistency while allowing platform-specific customization.
</Info>